# 更新日志

## [v0.2.1] - 2026-04-27

### 新增
- 系统托盘支持，右键菜单可快速访问主窗口、开始/停止录音、退出程序
- 启动行为配置：可选择启动时显示主窗口或仅显示托盘图标
- 关闭按钮行为配置：可选择最小化到托盘或退出程序
- PyInstaller 打包支持，生成独立可执行文件（`ReverieFlow.exe`）
- Windows 版本信息（文件属性、图标等）

### 改进
- 配置文件存储路径改为软件同级目录，支持便携模式（Portable）
- 音频设备列表延迟加载，点击下拉框时刷新设备状态
- 简化 `StreamingASR.close()` 方法，避免重复调用 `stop()`
- 录音开始时发送空结果信号，确保 UI 状态同步

### 技术架构
- 新增 `system_tray.py` 模块，封装系统托盘功能
- `main.py` 兼容开发环境和打包环境的路径处理（`sys._MEIPASS`）
- 新增 `ReverieFlow.spec` 和 `version_info.txt` 打包配置文件

---

## [v0.2.0] - 2026-04-26

### 新增
- 基于 qfluentwidgets 的全新 Fluent Design GUI 界面
- 主窗口采用左侧导航栏布局，包含"首页"和"设置"页面
- 全局快捷键 `Ctrl + Win`，快速开始/停止语音识别
- 悬浮提示窗口，快捷键模式下实时显示识别结果
- 润色完成后自动粘贴到当前活跃文本框，或复制到剪贴板
- 音频设备下拉选择，支持选择当前实际连接的麦克风
- GUI 设置页，通过图形界面配置 ASR API、文本润色 API 等参数
- JSON 配置文件（`config.json`）替代 `.env` 环境变量文件

### 改进
- 修复 Windows 下中文音频设备名乱码问题（使用 `sounddevice` 获取正确编码）
- 过滤已断开/未连接的音频设备，仅显示当前实际可用的设备（使用 `soundcard`）
- 音频设备列表按设备名去重，避免重复显示
- 悬浮窗口独立于主窗口，不跟随主窗口最小化
- 悬浮窗口宽度自适应（最小 200px，最大屏幕宽度 1/3），高度无上限
- 短文本自动居中，长文本自动换行纵向增长

### 技术架构
- 核心引擎管理（`EngineController`）：统一管理音频捕获、流式识别、文本润色的生命周期
- 信号槽机制：各模块间通过 Qt 信号槽通信，解耦设计
- 快捷键监听器（`HotkeyListener`）：基于 `pynput` 后台线程运行，不阻塞 UI
- `_use_overlay` 标志位：区分快捷键模式和按钮点击模式

### 依赖变更
- 新增：`qfluentwidgets`、`pynput`、`soundcard`、`sounddevice`、`pyperclip`、`pyautogui`
- 移除：`python-dotenv`（不再使用 `.env` 文件）

---

## [v0.1.0] - 2026-04-25

### 初始版本
- 流式 ASR 识别（基于千问 ASR 模型，通过 `dashscope` 调用）
- 文本润色功能（通过大模型 API）
- 基础命令行/简易 GUI 界面
- `.env` 环境变量配置方式
