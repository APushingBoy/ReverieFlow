import Foundation

enum AppGroup {
    static let suiteName = "group.com.reverieflow.ios"
    static let commandURLScheme = "reverieflow://request-mic"
}

enum SharedKeys {
    static let settings = "reverieflow.settings"
    static let pipelineSnapshot = "reverieflow.pipeline.snapshot"
}
